<div class="updatebox">

	{{#if eaten}}Enjoy your {{burgername}}!{{else}}Thank you for eating {{burgername}}!{{/if}}
	
{{#if eaten}}
	<button class="change-burger" data-id="{{id}}" data-eatthis="{{eaten}}">
		EAT ME NOW!
	</button>
<button class="delete-burger" data-id="{{this.id}}">Remove from your menu</button>
	{{else}}
	<button class="delete-burger" data-id="{{this.id}}">Throw your trash properly</button>
	
	
	{{/if}}
</div>
